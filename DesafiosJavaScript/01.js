const data = [
  { cliente: 'Ana', produto: 'Livro', valor: 30 },
  { cliente: 'João', produto: 'Caneta', valor: 5 },
  { cliente: 'Felipe', produto: 'Caderno', valor: 15 }
]

// const client = data.find(({ cliente }) => cliente === 'João')
// console.log(client)

// const client = data.filter(
//   cliente => cliente.valor > 5 && cliente.cliente === 'Ana'
// )
// console.log(client)

// const indexClient = data.findIndex(client => {
//   return client.cliente === 'João'
// })
// console.log(indexClient)
// console.log(data[indexClient].cliente = 'Fulano\n', data[indexClient])

// const clients = data.map(({ valor }) => valor * 2)
// console.log(clients)

// const total = data.reduce((acc, { valor }) => acc + valor, 0)
// console.log(total)

// console.log(data.reverse())

// const clients = data.some(({ cliente }) => cliente === 'João');
// console.log(clients)

// const clients = data.every(({ valor }) => valor > 4)
// console.log(clients)


